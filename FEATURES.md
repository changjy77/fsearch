# fsearch 기능 정리

fsearch는 PyQt5 기반 Windows 파일 검색 도구입니다. 지금까지 개발된 기능을 영역별로 정리합니다.

---

## 1. 핵심 검색 엔진

- **멀티스레드 병렬 검색**: `ThreadPoolExecutor` 기반, 스레드 수 1~32 조정 가능(기본 16)
- **검색 대상 선택**: 파일명만 / 내용만 / 둘 다(기본값)
- **정규식 검색**: 파일명·내용 모두에 정규식 패턴 적용 가능
- **대용량파일 스킵**: 10MB 이상 파일 제외 옵션
- **실시간 결과 스트리밍**: 검색 도중에도 결과가 즉시 테이블에 추가됨 (`result_found` 시그널)
- **검색 중단**: 진행 중인 검색을 언제든 중지 가능
- **폴더 제외**: 특정 하위 폴더(AppData, Program Files 등)를 검색에서 제외, 체크박스로 관리

---

## 2. 지원 파일 형식 (총 15개 확장자 + zip 내부 검색)

| 분류 | 확장자 | 비고 |
|---|---|---|
| 워드 | `.doc`, `.docx` | `.doc`는 파일명 검색만 지원(내용 파싱 불가) |
| 파워포인트 | `.ppt`, `.pptx` | `.ppt`는 파일명 검색만 지원 |
| 한글 | `.hwp`, `.hwpx` | 내부 zip 구조에서 XML 직접 추출 |
| PDF | `.pdf` | PyPDF2 사용 |
| 엑셀 | `.xls`, `.xlsx` | openpyxl(`.xlsx`) / xlrd(`.xls`) |
| 텍스트 | `.txt` | |
| 웹/마크다운 | `.html`, `.htm`, `.md` | |
| 데이터 | `.csv`, `.json`, `.xml` | 일반 텍스트로 읽음 |
| **압축파일** | `.zip` | **내부 문서까지 풀어서 검색** (지원 확장자 15종 그대로 적용) |

### docx 파싱 실패 우회
- python-docx가 대용량 임베디드 첨부(alternative format chunk 등)로 인해 파싱 실패할 경우, `word/document.xml`을 zip에서 직접 읽어 `<w:t>` 텍스트를 단락 단위로 추출하는 폴백 로직 적용
- 실제 9.9MB 임베디드 첨부 포함 문서에서 0자 → 1,824자 추출로 개선된 사례 있음

### zip 내부 검색
- zip 내부 각 파일도 지원 확장자면 파일명·내용 검색
- 결과는 `📦 압축파일명 → 📄 내부파일명` 형태로 표시 (더블클릭 시 zip 파일 자체가 열림)
- 내부 파일은 임시파일로 풀어 기존 추출 로직(`_extract_text_impl`)을 그대로 재사용

---

## 3. 성능 최적화 — 디스크 캐싱

- **텍스트 추출 결과 캐싱**: SQLite(`~/.fsearch/text_cache.db`)에 파일 경로+수정시간+크기를 키로 추출된 텍스트 저장
- **재검색 가속**: 같은 폴더를 다른 검색어로 재검색할 때 파싱을 건너뛰고 캐시된 텍스트 재사용
- **경로 프리픽스 스코프 로딩**: 검색 경로 하위 항목만 캐시에서 로드해, 캐시 전체를 매번 읽는 지연을 방지
- **zip 내부 파일 캐싱**: `zip경로!내부파일명`을 키로, 내부 파일의 CRC+크기 기준으로 변경 감지 → zip 하나가 안 바뀌어도 특정 내부 파일만 바뀌면 그 파일만 재추출
- **경로 구분자 정규화**: 캐시 키(`\`)와 조회 경로(`/` 또는 `\`)가 달라도 항상 매칭되도록 정규화 (하위 폴더 검색 시 캐시가 무효화되던 버그 수정)

> 참고: 삭제된 파일의 캐시 항목을 자동으로 정리하는 기능은 구현했다가 현재는 제거된 상태입니다(파일이 삭제돼도 캐시 항목은 자동으로 지워지지 않음).

---

## 4. 검색 결과 화면

### 테이블 탭
- 컬럼: 파일명 / 경로 / 크기 / 수정 날짜 / 검색 단어수
- 검색어는 파일명·경로에서 **굵은 빨간색**으로 강조 표시
- **행 호버 효과**: 마우스를 올리면 해당 행 전체가 진한 회색으로 표시, 벗어나면 원래 색으로 복원 (QLabel 강조 셀 포함 모두 처리)
- **더블클릭으로 파일 실행**: 검색 중에도 가능, 다단계 폴백으로 안정적으로 파일 경로를 찾아 실행
- **업무 연관도 기반 정렬**: 단순 매칭 횟수가 아니라 휴리스틱 점수로 정렬
  - 파일명에 검색어 포함: +1000점
  - 문서 파일(docx/hwp: 50점, pdf/xlsx: 40점, txt/md: 30점 등): 가중치 부여
  - 소스코드 파일(.py/.js/.cpp 등): −10점
  - 매칭 횟수: ×10점

### 텍스트 탭
- 검색 결과를 파일별로 상세 정보(경로/크기/수정일/검색 단어수)와 함께 텍스트로 표시
- 검색어 강조 동일 적용

### 미리보기(호버 시)
- 매칭된 라인 최대 4줄을 툴팁으로 미리보기
- **파일명 전용 검색 감지**: 검색 단어수가 1이고 본문에 매칭 라인이 없으면 "📝 본문에 검색어 없음(파일명만 일치)"로 별도 표시

---

## 5. 검색 이력 관리

- **최근 검색어**: 최대 10개, 중복 제거 후 최신순 저장(`~/.fsearch/search_history.json`)
- **최근 검색 경로**: 최대 5개, 동일하게 관리
- **검색어 초기화 버튼**: 저장된 모든 검색어를 확인 후 일괄 삭제
- **경로 초기화 버튼**: 저장된 모든 검색 경로를 확인 후 일괄 삭제(초기화 후 현재 디렉터리로 자동 설정)

---

## 6. 폴더 관리

- 자주 제외하는 폴더를 통계로 기록하고 상위 N개를 자동 노출
- 체크박스로 제외 폴더를 선택적으로 활성화/비활성화

---

## 7. 파일 실행

- 결과 더블클릭 시 원본 프로그램으로 파일 열기 (`os.startfile`)
- 행 번호-데이터 불일치 상황에 대비한 다단계 폴백(`self.results` → `UserRole` 데이터 → QLabel 속성)
- 파일 확장자별 적절한 기본 프로그램으로 실행되도록 처리

---

## 8. 부가 기능

- **제외된 파일 / 찾은 파일 수 / 검색어 미포함 파일 수 / 스킵된 대용량파일**: 각각 버튼으로 목록 확인 가능
- **완료시간 표시**: 검색 소요 시간을 버튼에 표시
- **자동 로깅**: `~/.fsearch/logs/`에 날짜별 로그 파일 생성, 검색 시작/완료 기록
- **파일 형식별 아이콘**: 테이블에 확장자별 이모지 아이콘 표시

---

## 9. 배포

- PyInstaller로 단일 실행파일(`dist/fsearch.exe`, 약 52MB) 빌드
- Python 미설치 환경에서도 바로 실행 가능

---

## 개발 중 도입했다가 제거된 기능 (참고용)

아래 기능들은 한 시점에 구현되었으나 이후 되돌려져 **현재 코드에는 포함되어 있지 않습니다**:

- 테이블/텍스트 탭 "모던 라이트" 스타일(줄무늬 배경, 헤더 색상, 선택 강조 등)
- 검색어 입력 시 자동으로 검색되는 인크리멘탈 서치(300ms 지연)
- 삭제된 파일의 캐시 항목을 검색 시마다 자동으로 정리하는 로직

---

*최종 정리 기준: 커밋 `0c51b92` (하위 폴더 검색 시 캐시가 무효화되던 버그 수정)*
